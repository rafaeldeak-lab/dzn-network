DZN badge asset root.

Place individual static and animated badge assets in the category folders below. The Phase 3 visual system can reference `*.webp` files, animated `*-animated.webp` files, or fall back to CSS-rendered icons when assets are not present.
