# DZN Public Access Policy

This policy separates public entry points from authenticated product pages. It is intentionally conservative: hidden navigation is not enough; direct URLs must match the same access model.

## Logged-Out Visitors

Logged-out visitors may access:

- `/`
- `/#features`
- `/pricing`
- `/login`
- `/signup`
- `/players/[handle]` when the player has explicitly enabled public profile visibility
- `/servers/[slug]/community` for the public-safe community member directory
- the public Discord invite link

Logged-out navigation must only expose the public funnel: homepage/features, pricing, Login/Signup, and Discord. It must not show dashboard, server browser, leaderboards, stats, events, owner tools, or add-server controls before a session is known.

Logged-out visitors are redirected to `/login?returnTo=...` before app-page rendering for:

- `/dashboard`
- `/dzn-pulse`
- `/events`
- `/leaderboards`
- `/player`
- `/seasons`
- `/servers`
- `/setup`
- `/test`

Nested routes under those paths follow the same rule unless explicitly listed as a public-safe shell above. Examples include `/events/suggest`, `/events/server-wars`, `/servers/profile?slug=...`, `/servers/[slug]`, `/dashboard/events`, and `/setup/...`.

## Logged-In Players

Logged-in Discord users are free DZN players by default. They may open player-facing authenticated app pages without choosing a paid owner plan:

- `/dzn-pulse`
- `/events`
- `/leaderboards`
- `/player`
- `/seasons`
- `/servers`

Player-facing and community surfaces may use the Discord OAuth `identify guilds` scope to match the player's Discord communities to DZN-connected servers. Player guild matching must not grant server-management rights and must not overwrite owner/admin guild records.

Starter trial and Pro behavior must continue to come from the billing/entitlement helpers and API responses. A page redirect must not replace owner authorization, plan enforcement, Stripe webhook checks, Nitrado ownership checks, or protected API 401/403 behavior.

Free player accounts should see player navigation plus a clear owner-plan action. Starter/trial accounts should see trial-safe owner navigation plus a clear Pro upgrade action. Pro-effective accounts should see Pro tools in the header. This header visibility is product guidance only; APIs and owner/product pages must continue enforcing access server-side.

Dashboard package visibility must follow the same split. Starter/trial users may see the normal setup, public listing, basic stats, events, billing comparison, and basic Discord posting surfaces, but any Pro-only analytics, promotion, Server VS Server hosting, or enhanced Discord post controls must read as locked or upgrade-gated. Pro-effective users may see those tools as active, while server-side entitlement checks remain authoritative.

Package copy must not imply a competitive advantage. Pro presentation, promotion, analytics, and owner tooling must not change leaderboard rank, K/D, score, reviews, crowns, badges, season wins, event outcomes, or gameplay results.

## Owner Setup And Server Management

Owner/server-management actions require Discord login plus an active or trialing owner entitlement from the canonical billing layer. Free Discord players must be redirected to `/pricing?intent=owner_setup&returnTo=%2Fsetup` for setup/dashboard pages and must receive an owner-plan-required API error for direct owner API calls.

Owner-gated pages include:

- `/setup`
- `/dashboard`

Owner-gated APIs include:

- `/api/onboarding/*`
- `/api/nitrado/*`
- `/api/server/*`
- `/api/servers/[serverId]/*`
- owner-triggered `/api/sync/*` calls, except cron-secret-authorized sync paths
- `/api/events/[slug]/join` and `/api/events/matchmaking` when acting on an owned server
- `/api/discord/bot-status`

Existing ownership, role, Nitrado, Discord admin, and Pro-feature checks still apply after the owner entitlement gate passes. A paid plan does not make a user the owner of someone else's linked server.

## Owner Pages

`/owner` and nested owner pages remain stricter than normal logged-in pages. They must continue to require platform-owner or platform-creator authorization through the owner page functions.

## Public APIs

The homepage and public preview surfaces still need public read-only JSON. These APIs remain callable without a session unless a later high-risk access redesign explicitly changes them:

- `/api/public/servers`
- `/api/public/home-stats`
- `/api/public/server-rail`
- `/api/public/leaderboards`
- `/api/public/leaderboards/advanced`
- `/api/public/server-wars`
- `/api/public/player-profiles/[handle]`
- `/api/public/servers/[serverId]/community-members`
- `/api/events`
- `/api/events/suggestions?sort=newest&limit=5`
- `/api/dzn-pulse/config`

Authenticated player APIs such as `/api/player/hub` and `/api/player/communities` may require a session while remaining free of owner billing requirements.

`/api/player/hub` is the free logged-in Player Hub payload. It may read matched Discord communities, saved/followed server preferences, public server suggestions, and public event/tournament suggestions. It must not create checkout sessions, call Nitrado, mutate Discord guild ownership, require owner entitlement, write billing state, or alter competitive/stat tables.

`/api/player/saved-servers` is the free logged-in save/follow preference endpoint. `GET` returns only the current player's saved public servers. `POST` and `DELETE` may write only the current player's row in `player_saved_servers` after resolving the target as a visible public server. Saving a server must not affect rankings, discovery score, billing, server ownership, reviews, events, tournaments, Server Wars, badges, XP, challenge outcomes, or competitive eligibility.

`/api/player/reviews` is the free logged-in review submission endpoint. It may create or update only the current player's review row in `server_reviews` for a visible public server, subject to review validation, moderation checks, owner self-review blocking, and cooldown rules. Review submission must not require Starter, Pro, owner entitlement, server ownership, Nitrado access, Stripe, Discord bot permissions, or billing state. Review submission must not affect rankings, discovery score, billing, server ownership, events, tournaments, Server Wars, badges, seasons, XP, challenge outcomes, calling cards, or competitive eligibility.

`/api/player/challenges` is the free logged-in challenge participation and progression endpoint. `GET` returns player challenge catalog rows, the current player's private joined/completed state, earned XP summary, and earned calling-card hooks. `POST` may join an active challenge by writing only the current player's row in `player_challenge_participations`. Player challenge participation must not affect rankings, discovery score, billing, server ownership, reviews, review score, events, tournaments, Server Wars scoring, badges, seasons, XP self-awards, calling-card self-awards, or competitive eligibility.

`/player/profile` and `/api/player/profile` are free logged-in player progression showcase surfaces. The page shows earned XP, challenge progress, calling cards, and a progression timeline from the existing player progression read model, with privacy display controls for private view, public-safe preview, and hidden preview. Public profile publishing stays off. The API must be `GET` only, private/no-store, session-scoped through `getRequestSessionUser`, and read-only. It may hydrate saved display choices from the current player's private profile privacy preferences. It must not require Starter, Pro, owner entitlement, server ownership, Nitrado access, Stripe, Discord bot permissions, or billing state, and must not expose Discord IDs, internal user IDs, source IDs, raw evidence blobs, billing rows, Nitrado tokens, Discord bot tokens, or Stripe state. Profile progression showcase visibility must not affect paid plans, rankings, discovery score, reviews, review score, badges, seasons, events, Server Wars scoring, server ownership, XP awards, calling-card awards, or competitive eligibility.

`/api/player/profile-privacy` is the private player-owned settings API for persistent profile visibility preferences. `GET` returns only the current player's saved privacy settings or safe defaults, and `PATCH` may write only the current player's row in `player_profile_privacy_preferences`. The endpoint must use `getRequestSessionUser`, private/no-store responses, bounded JSON parsing, and session-owned user binding. It must reject logged-out visitors, reject non-`GET`/`PATCH` methods, ignore body-supplied `user_id` or `discord_id` values, and must not require Starter, Pro, owner entitlement, server ownership, Nitrado access, Stripe, Discord bot permissions, or billing state. No public profile reader route is introduced by this settings API. Display preferences must not affect billing, rankings, discovery, reviews, badges, seasons, events, Server Wars scoring, XP awards, calling-card awards, or competitive eligibility.

`/players/[handle]` and `/api/public/player-profiles/[handle]` are the public-safe player profile viewer surfaces. They return only opted-in profiles where the player has enabled public profile visibility and saved a generated `public_handle`. The Cloudflare Pages route for `/players/[handle]` serves the exported viewer shell for arbitrary generated handles, and the public API remains the `GET`-only, read-only privacy boundary. It may read `player_profile_privacy_preferences`, `users`, and existing player progression summary tables to display only sections the player has approved: XP, joined/completed challenge progress, calling cards, and optional month/year award labels. Private identifiers, Discord IDs, internal user IDs, source IDs, raw award evidence, and exact award timestamps stay hidden. Public profile display choices must not affect billing, rankings, discovery, reviews, badges, seasons, events, Server Wars scoring, XP awards, calling-card awards, or competitive eligibility.

The public profile discovery/linking polish slice adds public profile entry links from private player surfaces such as `/player`, `/player/profile`, `/events/challenges`, and DZN Pulse. `GET /api/player/hub` may read only the current player's profile privacy row to return a `public_profile` link summary for the authenticated player. Copy/share controls are local browser actions for the profile owner only and may not create handles, write profile settings, create checkout sessions, touch billing, update rankings, mutate reviews, award XP/calling cards, alter events, change Server Wars scoring, call Nitrado, change Discord resources, or expose private identifiers. Public profiles remain read-only and hidden/not-yet-earned sections must use public-safe empty states.

The public profile cross-surface attribution slice may show generated public profile links on review author rows, current-player challenge participation rows, and safe leaderboard player mentions only when the underlying row resolves to a DZN user with `public_profile_enabled = 1` and a saved `public_handle`. Leaderboard aggregates must resolve to exactly one DZN user before linking; ambiguous aggregates or conflicting kill/death bindings render as plain names. Attribution must be read-only, must not create handles, must validate exact generated-handle profile hrefs, and must not infer identity from matching player names, review names, gamertags, or request-supplied handles. Hidden, unpublished, malformed, missing, or unconfigured profiles must render without public profile links; public review authors without published profiles remain generic DZN player rows. Attribution links must not affect billing, rankings, discovery score, review score, badges, seasons, events, Server Wars scoring, XP awards, calling-card awards, or competitive eligibility.

The public profile attribution expansion and controls polish slice adds a private "where my public profile appears" preview/control surface on logged-in player pages and extends opt-in attribution to public event suggestion author rows only through the trusted `event_suggestions.submitted_by_user_id` to `users.id` bridge. The preview is controlled by the player's existing `public_profile_enabled` setting, and the only write remains the existing private profile privacy save action. CTF/event scoring rosters, owner event workflows, and owner/admin review tools remain excluded until a dedicated slice proves links are presentation-only. This slice must not create handles outside the existing profile-privacy flow, infer identity from display names or gamertags, write rankings, update discovery score, mutate reviews, modify events, change Server Wars scoring, award XP/calling cards, change billing, call Nitrado, mutate Discord resources, enable live checkout, or merge issue #49.

The CTF/event roster attribution proof slice allows optional public profile links only on read-only CTF dashboard roster display rows. The trusted bridge is exact `ctf_tournament_rosters.linked_server_id` plus `ctf_tournament_rosters.player_id` to `player_profiles.discord_id`, then `users.discord_id`, then an opted-in `player_profile_privacy_preferences.public_handle`. The dashboard must not infer links from gamertags or display names, expose internal user IDs or Discord IDs, make roster data public, write roster rows, alter registration, update scoring, affect eligibility, change owner decisions, touch billing, update rankings/discovery/reviews/badges/seasons/events/Server Wars/progression awards, call Nitrado, mutate Discord resources, enable live checkout, or merge issue #49. CTF roster registration, CTF scoring gates, accepted audit feeds, event approval flows, owner management rows, and admin/moderation flows remain out of scope unless a later slice proves a separate presentation-only boundary.

The event roster/member public-safe expansion slice adds opt-in public profile attribution only to public event host/member display rows backed by the trusted `competitive_events.created_by` to `users.id` bridge, exposed as `public_event_creator_member_rows`. The public events list, event detail hero, event cards, event tables, and public server event profile cards may show a generated-handle profile link when the creator has `public_profile_enabled = 1` and a valid `public_handle`. Hidden, unpublished, malformed, missing, or unconfigured event creators render without a profile link, and public payloads must not expose raw `created_by`, internal user IDs, Discord IDs, requested handles, or gamertag-derived identity. Registered server rows, event leaderboards, CTF scoring rows, owner workflow rows, approval decisions, bracket outcomes, billing, rankings, discovery score, reviews, badges, seasons, Server Wars scoring, XP awards, calling-card awards, and competitive eligibility remain isolated and cannot read or write this attribution state.

The public-safe community member directory foundation adds `/servers/[slug]/community` and `GET /api/public/servers/[serverId]/community-members` as read-only public-safe surfaces. The trusted bridge is `community_members.community_guild_id` plus `community_members.user_id` to `users.id`, then an opted-in `player_profile_privacy_preferences.public_handle`. The route may show only members with `public_member_enabled = 1`, `source = 'trusted_dzn_bridge'`, and a published generated handle. It must not infer identity from Discord names, gamertags, review names, leaderboard names, or request-supplied handles, and it must not expose raw community guild IDs, raw user IDs, Discord IDs, OAuth tokens, server ownership state, billing state, approval state, scoring state, raw award evidence, or owner workflow state. CTF scoring rows, owner workflow rows, approval decisions, bracket outcomes, billing, rankings, discovery score, reviews, review score, badges, seasons, events, Server Wars scoring, XP awards, calling-card awards, and competitive eligibility remain isolated and cannot read or write this directory state.

The community member directory and Player Hub surfacing polish slice may add `community_href` links to `GET /api/player/hub` server summaries, render a Player Hub "Community Member Directories" section for matched/saved/suggested public servers, add public directory search and role filters across already-public profile rows, and show public community URL copy/share controls. These are presentation-only read paths. Search and filtering must not query hidden members, raw source candidates, raw Discord IDs, raw DZN user IDs, owner workflow state, billing state, scoring rows, review internals, award evidence, or private profile settings. Owner/admin community member source cards may preview whether a linked candidate is public-ready, but that public directory preview is presentation-only and cannot publish a player profile; player opt-in through the generated public profile handle remains required. This slice must not add retained export files, export-history rows, sharing links, storage bindings, retention write APIs, retained-export migrations, live checkout activation, Stripe product/price changes, Cloudflare secret changes, production D1 writes, Nitrado calls, Discord mutations, or issue #49 merges.

The Public Community Directory Discovery Polish Slice may add richer sorting, grouping, and safe context cards to `/servers/[slug]/community`. These controls sort and group already-visible public rows only, and the slice sorts and groups already-visible public rows only. They may use public display names, public role labels, public month-level member-since labels, generated public handles, and public profile hrefs that were already returned by `GET /api/public/servers/[serverId]/community-members`. They must not query hidden members, raw source candidates, owner/admin import rows, raw Discord IDs, raw DZN user IDs, raw linked-server IDs, raw community guild IDs, owner workflow state, billing state, scoring rows, review internals, award evidence, private profile settings, or retained export artifacts. The controls are presentation-only and must not affect billing, rankings, discovery score, reviews, badges, seasons, events, Server Wars scoring, XP awards, calling-card awards, or competitive eligibility. Live checkout remains disabled, and Issue #49 remains reserved for final live payment activation.

The Public Community Member Card Preview Polish Slice may add compact preview metadata to public member cards from each member's already-published profile sections only. The preview is valid only when DZN already has the trusted `community_members.community_guild_id` plus `community_members.user_id` bridge, the player has an opted-in generated public profile handle, and that exact profile section is visible under the player's saved privacy settings. The preview may show public-safe section labels and counts for visible XP, challenge progress, and calling cards. Hidden profile sections stay private and must not be queried for the card preview. The preview must not expose raw Discord IDs, internal DZN user IDs, raw source IDs, raw award evidence, exact award timestamps, owner/admin import records, retained export artifacts, billing state, scoring state, review internals, approval state, or private profile settings. Retained exports remain blocked, owner/admin import controls remain separate, live checkout remains disabled, and Issue #49 remains reserved for final live payment activation. The card preview must not affect billing, scoring, rankings, discovery score, reviews, badges, seasons, events, Server Wars scoring, XP awards, calling-card awards, or competitive eligibility.

The Player Public Profile Visual Polish Slice may make `/players/[handle]` itself feel more DZN-branded with cinematic background layers, public dossier styling, stronger visible-section summaries, and subtle reduced-motion-aware animation. This is presentation-only. The page must keep using `GET /api/public/player-profiles/[handle]`, must not write profile privacy settings, must not create profile handles, must not expose hidden sections, and must not show private identifiers, Discord IDs, internal user IDs, source IDs, raw award evidence, or exact award timestamps. Public profile styling cannot affect billing, scoring, rankings, reviews, badges, seasons, Server Wars, XP awards, calling-card awards, or competitive eligibility. Live checkout remains disabled, retained exports remain blocked unless separately approved, and Issue #49 remains reserved for final live payment activation.

The Public Profile Owner Preview and Share Polish Slice may improve the private `/player/profile` share panel with a "How My Public Profile Looks" preview, clearer copy/share controls, and hidden-section or unsaved-change warnings for the profile owner. The private owner preview may use only the already-loaded authenticated player profile payload and the current local visibility controls; it must not fetch public profile data, write privacy settings, create handles, create checkout sessions, touch billing, update rankings, mutate reviews, award XP/calling cards, alter events, change Server Wars scoring, call Nitrado, change Discord resources, or expose private identifiers. The only write on `/player/profile` remains the explicit Save Preferences action against `/api/player/profile-privacy`. Copy/share controls may only copy the generated public link or handle locally, open the existing public href, or invoke the browser share sheet. Owner preview/share UI is presentation-only and cannot affect profile privacy settings, billing, scoring, rankings, reviews, badges, seasons, Server Wars, XP awards, calling-card awards, events, or competitive eligibility. Live checkout remains disabled, retained exports remain blocked unless separately approved, and Issue #49 remains reserved for final live payment activation.

The Public Profile Share Session Feedback Slice may show a private "This Page Session" panel inside the logged-in `/player/profile` share panel with the last public page open, public link copy, public handle copy, and browser share-sheet open for the current mounted page only. This feedback may use only local component state and must disappear on reload, navigation, tab close, or remount. It must not use localStorage, sessionStorage, IndexedDB, cookies, browser beacons, analytics events, tracking events, audit-log calls, profile-privacy writes, public-profile fetches, or any server route. It must show that the feedback is private to this tab and is not saved or sent to DZN. Session feedback is presentation-only and cannot affect profile privacy settings, billing, scoring, rankings, reviews, badges, seasons, Server Wars, XP awards, calling-card awards, events, or competitive eligibility. Live checkout remains disabled, retained exports remain blocked unless separately approved, and Issue #49 remains reserved for final live payment activation.

The Public Profile Share Accessibility/Fallback Polish Slice may improve the same private `/player/profile` share panel with clearer keyboard and screen-reader affordances, accessible labels and descriptions, focus-visible states, an `aria-live` share status region, browser capability checks, and fallback guidance when Clipboard copy is unavailable, public-handle copy is unavailable, or Browser share is unavailable. The fallback guidance may direct the player to open the public profile page and copy the address bar link, or use Copy Link when native browser share is unavailable. It may use only local component state and browser capability checks. It must not add stored share history, tracking events, analytics calls, audit-log calls, localStorage, sessionStorage, IndexedDB, cookies, beacons, fetches, API calls, profile-privacy writes, or any server route. The only write on `/player/profile` remains the explicit Save Preferences action against `/api/player/profile-privacy`. Accessibility and fallback polish is presentation-only and cannot affect profile privacy settings, billing, scoring, rankings, reviews, badges, seasons, Server Wars, XP awards, calling-card awards, events, or competitive eligibility. Live checkout remains disabled, retained exports remain blocked unless separately approved, and Issue #49 remains reserved for final live payment activation.

The Public Profile Share Preview Metadata Polish Slice may add public-safe Open Graph/Twitter-style metadata and fallback preview copy to `/players/[handle]`. The Next static route may define only generic fallback metadata for the exported preview shell; per-handle metadata must be generated by the Cloudflare Pages `functions/players/[handle].ts` shell route from the same already-filtered public profile payload used by `GET /api/public/player-profiles/[handle]`. Metadata may include display name, public handle URL, visible XP level/XP summary, visible challenge completion summary, and visible calling-card count only when those sections are already public under the player's saved visibility preferences. Hidden sections, private identifiers, Discord IDs, internal user IDs, source IDs, raw award evidence, exact award timestamps, private profile settings, owner/admin rows, retained export artifacts, billing state, scoring rows, review internals, approval state, event internals, and source evidence must stay hidden. The metadata path must not store share history, create tracking events, call analytics, write profile privacy settings, create profile handles, create checkout sessions, update billing, mutate rankings, change discovery score, mutate reviews, award badges, alter seasons, mutate events, change Server Wars or CTF scoring, award XP, award calling cards, call Nitrado, mutate Discord resources, change Cloudflare secrets, apply production D1 migrations, enable live checkout, or merge Issue #49.

The Public Profile Share Preview Crawler QA Slice may add a local smoke harness that calls the real `/players/[handle]` Pages shell handler with fake static assets and a read-only fake D1 binding, renders crawler-visible HTML for published, hidden, invalid, and unavailable profiles, and snapshots the final `<head>` metadata. The harness may assert title, description, canonical, robots, Open Graph, Twitter, DZN fallback preview copy, preview source, content/cache headers, duplicate managed-tag state, asset-shell path, body-shell preservation, and write-query count. It must not call production D1, Cloudflare secrets, live Stripe, Nitrado, Discord, analytics, tracking, share-history storage, privacy writes, retained export storage, deployments, or issue #49. Crawler QA must continue proving `/players/[handle]` metadata renders without hidden fields, analytics/tracking calls, share-history storage, privacy writes, billing changes, scoring changes, ranking changes, review changes, badge/season/Server Wars changes, XP/calling-card award changes, event changes, or competitive eligibility impact.

The Public Profile Share Preview Image/Card Polish Slice may add a canonical static share-card asset catalog and local image quality check for `/players/[handle]` metadata. `/media/dzn-cinematic-survivor.png` and any future DZN share-card asset references must be root-relative public `/media/*` PNG, JPEG, or WebP assets, must exist under `public/`, must be present under `out/` when a local static export exists, must meet crawler-friendly dimensions, and must have public-safe alt text. Future missing or unsafe card candidates must fall back to the default DZN cinematic survivor card. Share-card image metadata must not expose hidden profile sections, private identifiers, Discord IDs, internal user IDs, source IDs, raw award evidence, exact award timestamps, private settings, owner/admin rows, retained export artifacts, billing state, scoring rows, review internals, approval state, event internals, CTF scoring, or Server Wars scoring. The image/card QA path must not store share history, create tracking events, call analytics, write profile privacy settings, create profile handles, create checkout sessions, update billing, mutate rankings, change discovery score, mutate reviews, award badges, alter seasons, mutate events, change Server Wars or CTF scoring, award XP, award calling cards, call Nitrado, mutate Discord resources, change Cloudflare secrets, apply production D1 migrations, enable live checkout, or merge Issue #49.

The Public Profile Share-Card Crawler Visual QA Slice may render deterministic local Open Graph and Twitter social-card preview models from the final rewritten `/players/[handle]` head for published, hidden, invalid, unavailable, and fallback-image states. These rendered previews must use the correct final image URL and alt text, must use only public-safe static share-card assets, must keep hidden/invalid/unavailable profiles on generic `noindex,nofollow` fallback metadata, and must not expose hidden profile sections, private identifiers, raw award evidence, private settings, owner/admin rows, retained export artifacts, billing state, scoring rows, reviews internals, events internals, CTF scoring, or Server Wars scoring. The visual QA path must not store share history, create tracking events, call analytics, write profile privacy settings, create checkout sessions, update billing, mutate rankings, discovery score, reviews, badges, seasons, events, Server Wars, CTF scoring, XP awards, calling-card awards, call Nitrado, mutate Discord resources, change Cloudflare secrets, apply production D1 migrations, enable live checkout, or merge Issue #49.

The Public Profile Social Preview Validation Package Slice may create deterministic local JSON and HTML artifacts containing sanitized rendered head/card snapshots for `/players/[handle]` published, hidden, invalid, unavailable, and fallback-image states. These artifacts are reviewer evidence only. They may include sanitized final `<head>` HTML, crawler-visible Open Graph/Twitter card values, local static image preview references, and explicit false safety flags. They must not include scripts, forms, browser storage, beacons, analytics calls, tracking calls, share-history storage, audit-share calls, API write methods, checkout creation paths, live-service token names, raw SQL writes, production mutation commands, hidden profile sections, private identifiers, raw award evidence, private settings, owner/admin rows, retained export artifacts, billing state, scoring rows, reviews internals, events internals, CTF scoring, or Server Wars scoring. They must not require production D1, Cloudflare secrets, Stripe, Nitrado, Discord, retained export storage, deployment, live checkout activation, or issue #49.

Future DZN global/community/private chat and support chat must be designed in a dedicated slice before implementation. Support entry points may appear on most pages, logged-in players may use future community chat, and trusted groups may later receive private chat, but participation, moderation, and membership must be scoped server-side. Any automated support bot must answer only from public DZN website, setup-help, pricing, and support-policy content; it must not use private player data, private owner data, hidden profile sections, raw award evidence, Nitrado tokens, Discord IDs, billing secrets, production D1 internals, or retained export artifacts. Community chat must include profanity filtering, warning, timed-mute/timeout, report, and moderation hooks. No AI provider credential, paid API key, metered model call, vector store, training job, or automated spend path may be added until a dedicated approved support-bot architecture slice defines provider choice, cost controls, data boundaries, source policy, abuse handling, logging, and rollback. Chat and support bot features must remain isolated from billing, scoring, rankings, discovery, reviews, badges, seasons, events, Server Wars, XP awards, calling-card awards, and competitive eligibility.

Future DZN global/community/private chat and support chat are still blocked from real runtime implementation until the approved contracts exist. The DZN Chat And Support Architecture Preflight Slice records the access model: Support entry points may be visible on most pages, global community chat must require Discord login for participation, and private group chat must require a trusted DZN membership bridge. Free logged-in players may participate in allowed player/community chat without Starter or Pro. Server-linked group management and owner/community moderation must be owner/admin scoped and must still use canonical owner entitlement plus linked-server ownership where the action manages an owned server community. The AI support bot must answer only from public DZN website, setup-help, pricing, and support-policy content, and it must refuse private player data, private owner data, hidden profile sections, raw award evidence, Nitrado tokens, Discord IDs, billing secrets, production D1 internals, retained export artifacts, internal moderation notes, and private chat history outside the active support session. Chat moderation must include profanity filtering, warning, timed-mute/timeout, report, and staff-review hooks. No runtime chat route, Durable Object/WebSocket, moderation table, bot prompt, vector store, AI provider credential, metered model call, analytics/tracking path, or stored chat/support history is implemented by the preflight. Chat and support must not affect billing, scoring, rankings, discovery score, reviews, review score, badges, seasons, events, Server Wars, XP awards, calling-card awards, or competitive eligibility.

The DZN Comms Visual Shell Prototype Slice may add a static `/community` page and a site-wide DZN Assist launcher using static local mock data, disabled/non-sending composer controls, Website support only copy, and no message persistence. Community may appear in authenticated player navigation as a free player/community surface. The prototype must add No runtime chat APIs, No Durable Objects/WebSockets, No moderation tables, No bot prompts, No vector stores, No AI provider credentials, No metered model calls, and No analytics/tracking. The prototype cannot affect billing, scoring, rankings, discovery score, reviews, review score, badges, seasons, events, Server Wars, CTF scoring, XP awards, calling-card awards, or competitive eligibility. Live checkout remains disabled and Issue #49 remains reserved for final live payment activation. Next should be the DZN Comms interaction contract and moderation preflight before any runtime chat, persistence, transport, moderation, or support-bot implementation.

The DZN Comms Interaction Contract And Moderation Preflight Slice may define future send attempt, filtering decision, warning/timeout state, read-only history, message report, owner/admin moderation scope, private group membership proof, support source policy, logging/retention, and rollback contracts. It may name future client/server shapes such as `clientMutationId`, accepted/blocked/warning/timeout/rate-limited/muted/unauthenticated/forbidden responses, no-store authenticated history, cross-owner denial, and design-only DZN Comms kill switches, but it must not implement runtime chat APIs, message tables, chat message database migrations, Durable Objects/WebSockets, moderation tables, bot prompts, vector stores, AI provider credentials, metered model calls, analytics/tracking, stored support/chat history, live checkout changes, or production service mutations. Free logged-in players can participate in future allowed DZN Comms player/community chat without Starter or Pro. Private group chat must require a trusted DZN user ID bridge. Owner/community moderation must require canonical owner entitlement plus linked-server ownership for owner-managed scopes, while DZN admins use separately configured global moderation scope. DZN Assist must stay limited to public DZN website content, setup-help content, pricing content, public support policy, public event guides, and public feature documentation until a later approved support-bot implementation defines provider, cost, source, retention, refusal, and rollback controls. Warnings, timeouts, mutes, reports, moderation status, private group visibility, DZN Assist support state, and future chat history must not affect billing, owner entitlements, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, Server Wars, CTF scoring, XP awards, calling-card awards, public profile visibility, retained exports, or competitive eligibility. Live checkout remains disabled and Issue #49 remains reserved for final live payment activation. Next should be the DZN Comms runtime implementation approval preflight before any chat APIs, message tables, Durable Objects/WebSockets, AI provider credentials, vector stores, or metered model calls are implemented.

The DZN Comms Runtime Implementation Approval Preflight Slice may choose the first runtime direction and add the requested public live website counter to the plan. The approved first runtime direction is a public-safe aggregate "DZN online" presence counter, preferably first on `/community` or the Global Chat shell, with later site-header or major-page placement only after privacy and fallback proof. This preflight may define future presence read/write flags, a short-lived heartbeat or connection model, aggregate response shape, TTL, fallback copy, transport sequence, migration choices, retention defaults, moderation separation, testing matrix, and rollback path, but it must not implement chat APIs, support chat APIs, presence APIs, live visitor counter APIs, message tables, presence tables, database migrations, Durable Objects/WebSockets, moderation tables, AI provider credentials, vector stores, metered model calls, analytics/tracking, live checkout changes, or production service mutations. The future counter must be presence, not analytics. It must not store browsing history, route history, user journeys, marketing events, tracking events, referrers, IP addresses, user agents, Discord identifiers, raw DZN user IDs, profile handles, billing state, owner entitlement, Nitrado identifiers, review identifiers, event identifiers, challenge identifiers, or competitive identifiers. It must not affect billing, owner entitlements, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, Server Wars, CTF scoring, XP awards, calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility. Live checkout remains disabled and Issue #49 remains reserved for final live payment activation. Next should be the DZN Comms live presence counter foundation before any chat message sending, message persistence, moderation-table implementation, DZN Assist AI runtime, AI provider credentials, vector stores, or metered model calls.

The DZN Comms Live Presence Counter Foundation Slice may add `/api/dzn-comms/presence`, a minimal `dzn_comms_presence_sessions` migration, and a DZN-branded counter on `/community` or the Global Chat shell. Runtime reads and writes must be behind disabled-by-default flags: `DZN_COMMS_PUBLIC_ONLINE_COUNTER_ENABLED`, `DZN_COMMS_PRESENCE_READ_ENABLED`, `DZN_COMMS_PRESENCE_WRITE_ENABLED`, and the client display flag `NEXT_PUBLIC_DZN_COMMS_PUBLIC_ONLINE_COUNTER_ENABLED`. When the flags are disabled, the UI must show a static fallback and avoid heartbeat requests. When enabled later, the route may store only an opaque hashed short-lived presence-session key, normalized scope, first seen timestamp, last seen timestamp, and expiry timestamp. The public response must be aggregate-only and no-store, with no names, Discord IDs, raw DZN user IDs, profile handles, IP addresses, user agents, referrers, routes, page history, journey history, billing state, owner entitlement, server ownership, Nitrado identifiers, review identifiers, event identifiers, challenge identifiers, scoring identifiers, or competitive identifiers. The counter must not implement message sending, message persistence, message history, private group messages, reports, moderation mutations, DZN Assist AI runtime, bot prompts, vector stores, AI provider credentials, metered model calls, analytics/tracking events, retained exports, live checkout changes, or production service mutations. The counter cannot affect billing, owner entitlements, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, Server Wars, CTF scoring, XP awards, calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Safe Monetisation And Supporter System Backlog supersedes the earlier paid-spin idea. Spins must be earned only through legitimate website activity such as daily activity, challenges, community missions, events, account milestones, and occasional free promotional awards; players must never be able to buy spins with money, paid credits, Supporter Packs, subscriptions, or bundles. Future wheel implementation must enforce maximum three total spins in any rolling 24-hour period, a minimum four-hour cooldown between spins, server-side spin generation, an auditable player/source/outcome/timestamp ledger, full reward-pool probability display, no empty spins, no cash-equivalent rewards, no transfer/sale/trade/redemption, and no fake near-miss or spending-pressure design. The future DZN Store may sell guaranteed account-bound digital products such as the `DZN FOUNDING SUPPORTER PACK`, profile themes, calling-card packs, chat/profile cosmetics, group banner/insignia packs, and event presentation themes only when every product clearly shows the exact contents before payment. Purchases must never grant XP, ranking advantages, better reward odds, extra spins, tournament advantages, review or discovery advantages, Server War scoring advantages, competitive eligibility, or gameplay power. Future Stripe or provider-backed implementation must use signed webhook verification, idempotent fulfilment, duplicate-event protection, order and entitlement ledgers, refund/chargeback handling, tax/VAT-compatible records, admin-configurable availability/pricing, purchase/refund terms, and no DZN storage of card information. This backlog item is not implemented by the presence-counter slice and does not change live checkout, Stripe products/prices, Cloudflare secrets, production D1, Nitrado, Discord, retained exports, rankings, scoring, XP, calling-card awards, reviews, discovery, seasons, events, or issue #49.

The DZN Safe Monetisation And Supporter System Implementation Preflight Slice may define the real production store/catalog/order/payment/spin-ledger/supporter-card implementation sequence, migration shapes, disabled-by-default flags, one-time Checkout `mode=payment` boundary, raw-body signed webhook verification, idempotent fulfilment, refund/chargeback revocation, admin pricing controls, tax/receipt record boundaries, rollback, and security proof. It must not implement account purchases, reward wheel, store checkout, store webhook fulfilment, product/order/payment/spin migrations, account entitlement writes, Supporter Card issuance, earned-spin ledgers, Stripe product/Price changes, Cloudflare secret changes, production D1 writes, live checkout activation, analytics/tracking, AI provider credentials, vector stores, metered model calls, or issue #49 changes. Future store purchases are player/account cosmetics only and must not unlock `/setup`, Nitrado linking, owner onboarding, owner dashboards, server-management APIs, billing plan status, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility. The first safe implementation step after the preflight is the DZN Store catalog and admin product/price draft model with disabled-by-default local migrations and admin validation only. The `/store` route is a public-safe, read-only DZN Store preview contract. It is disabled by default through the Store preview contract state. It may render safe catalog preview metadata only.

The DZN Store catalog and admin product/price draft model slice may add only the inactive `store_products` and `store_prices` schema plus `functions/_lib/dzn-store-catalog.ts`. The catalog helper is local validation only: it requires Store/admin flags and an already admin-authorized caller, rejects active product/price drafts, rejects Stripe Price IDs, and rejects any paid spin, XP, rank, discovery, review, event, Server Wars, CTF, owner setup, Nitrado, or competitive eligibility benefit. No account purchases, checkout, webhook fulfilment, account entitlement, supporter-card issuance, earned-spin, or wheel runtime route is introduced.

The DZN Store public browse and Supporter Card preview contract slice may add `/store` as a public-safe, read-only route that renders preview-only product cards, guaranteed-purchase labels, account-bound labels, no-competitive-advantage labels, and sample-only Supporter Card preview copy. The page may show that checkout is disabled and may link players back to `/player` or owner users to `/pricing?intent=owner_setup&returnTo=%2Fsetup`. It must not create checkout sessions, create orders, record payment events, grant account entitlements, issue supporter cards, grant earned spins, run a reward wheel, bind Stripe prices, enable live checkout, write production D1, mutate Cloudflare secrets, call Nitrado, mutate Discord resources, create analytics/tracking events, or merge issue #49. It must not affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store sandbox order and checkout approval preflight slice may define future authenticated `POST /api/store/orders` and future `POST /api/stripe/store-webhook` contracts, one-time Stripe Checkout `mode=payment` shape, payment event ledger rules, idempotent fulfilment rules, refund/chargeback revocation, tax/receipt record boundaries, disabled-by-default feature flags, rollback, and proof requirements. It must not add checkout routes, order tables, payment webhook handlers, entitlement writes, Supporter Card issuance, earned-spin ledgers, wheel runtime, Stripe object mutation, Cloudflare secret/config mutation, production D1 writes, live checkout activation, or issue #49 changes. It must not affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store sandbox order ledger schema slice may add `migrations/0072_dzn_store_order_ledger_schema.sql` with `store_orders`, `store_order_items`, and `store_payment_events`. The tables are private local/sandbox ledger tables only: `store_orders` is fixed to `livemode = 0`, `store_order_items` keeps one account-bound guaranteed item with no-competitive-advantage constraints, and `store_payment_events` stores unique provider event ids, raw event hashes, sanitized summaries, and fulfilment/write blockers fixed to `0`. It must not add checkout routes, Store APIs, webhook handlers, entitlement writes, Supporter Card issuance, earned-spin ledgers, wheel runtime, Stripe object mutation, Cloudflare secret/config mutation, production D1 writes, live checkout activation, or issue #49 changes. The schema cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store sandbox order creation route approval slice may add authenticated `POST /api/store/orders`. It is not a public checkout route. It is disabled by default and may write only pending local/test `store_orders` and `store_order_items` after session auth, explicit Store sandbox flags, local/test runtime, live-checkout blockers, and catalog safety checks pass. It must not expose raw Discord ids, Stripe ids, tax internals, or private payment state. It must not create Stripe Checkout Sessions, process Store webhooks, write `store_payment_events`, grant entitlements, issue Supporter Cards, mint earned spins, run the wheel, mutate Stripe, mutate Cloudflare secrets/config, write production D1, enable live checkout, or change issue #49.

The DZN Store sandbox Checkout Session approval slice may add authenticated `POST /api/store/orders/:orderId/checkout`. It is private to the purchasing logged-in player and must return `404` for missing or cross-user orders. It is disabled by default and may create a test-mode only Stripe Checkout Session only after session auth, explicit Store sandbox flags, `DZN_STORE_SANDBOX_CHECKOUT_SESSION_ENABLED=true`, `STRIPE_SECRET_KEY` test-mode proof, live-checkout blockers, an owned draft local/test Store order, a server-controlled test Price binding, order-derived idempotency, and catalog/item/order safety checks all pass. It may update only `store_orders` to `checkout_created`. It must not expose raw Discord ids, Stripe customer ids, PaymentIntent ids, webhook event ids, private payment state, or tax internals. It must not process Store webhooks, write `store_payment_events`, grant entitlements, issue Supporter Cards, mint earned spins, run the wheel, mutate Stripe Products/Prices/Customers/webhook endpoints/refunds/disputes, mutate Cloudflare secrets/config, write production D1, enable live checkout, or change issue #49. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, badges, seasons, Server Wars scoring, CTF scoring, XP awards, calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store sandbox webhook event ledger receipt slice may add `POST /api/stripe/store-webhook`. It is disabled by default, authenticated only by verified Stripe signature, and records only sanitized test-mode `store_payment_events` receipt rows after `DZN_STORE_SANDBOX_WEBHOOK_RECEIPT_ENABLED=true`, local/test Store sandbox flags, live-checkout blockers, and no-fulfilment runtime blockers pass. It must not fulfil orders, grant entitlements, issue Supporter Cards, mint earned spins, run the wheel, mutate Stripe Products/Prices/Customers/refunds/disputes/webhook endpoints, mutate Cloudflare secrets/config, write production D1, enable live checkout, or change issue #49. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store webhook fulfilment approval preflight slice is documentation/test-guard work only. It may define future verified test-mode fulfilment rules for `checkout.session.completed`, delayed-payment `checkout.session.async_payment_succeeded`, order-status transitions, exactly-once account entitlements, exactly-once Supporter Cards, refund/chargeback rollback, future schema preconditions, and proof requirements. It must not implement fulfilment route writes, account entitlement tables, Supporter Card tables, earned-spin ledgers, wheel runtime, live checkout activation, Stripe Product/Price/customer/refund/dispute/webhook endpoint mutation, Cloudflare secret/config mutation, production D1 writes, or issue #49 changes. The current Store webhook remains receipt-only, and `store_payment_events` fulfilment/write blockers remain fixed to `0`. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store fulfilment ledger schema migration approval preflight slice is documentation/test-guard work only. It defines the future local/test schema contract for account entitlements, Supporter Cards, fulfilment attempts, order status history, entitlement status history, and refund/dispute revocation audit before any migration file is added. It must not implement account entitlement tables, Supporter Card tables, Store fulfilment route writes, earned-spin ledgers, wheel runtime, live checkout activation, Stripe Product/Price/customer/refund/dispute/webhook endpoint mutation, Cloudflare secret/config mutation, production D1 writes, or issue #49 changes. The current Store webhook remains receipt-only, and `store_payment_events` fulfilment/write blockers remain fixed to `0`. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store fulfilment ledger schema migration slice may add `migrations/0073_dzn_store_fulfilment_ledger_schema.sql` as local/test-only private Store fulfilment ledger schema for account entitlements, Supporter Cards, fulfilment attempts, order-status history, entitlement-status history, and refund/dispute audit. It must not add runtime fulfilment, Store webhook fulfilment writes, Supporter Card issuance, earned-spin ledgers, reward wheel runtime, live checkout activation, Stripe Product/Price/customer/refund/dispute/webhook endpoint mutation, Cloudflare secret/config mutation, production D1 writes, or issue #49 changes. The schema is private infrastructure only and cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store fulfilment runtime implementation approval preflight slice is documentation/test-guard work only. It may define the future disabled-by-default local/test fulfilment runtime contract, exact write scope, idempotency, order-status transitions, account-entitlement creation rules, optional Supporter Card issuance rules, refund/chargeback rollback rules, test matrix, rollback path, and security proof in `docs/DZN_STORE_FULFILMENT_RUNTIME_IMPLEMENTATION_PREFLIGHT.md`. It must not add Store fulfilment runtime, account entitlement writes, Supporter Card issuance, earned-spin ledgers, reward wheel runtime, live checkout activation, Stripe Product/Price/customer/refund/dispute/webhook endpoint mutation, Cloudflare secret/config mutation, production D1 writes, or issue #49 changes. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store fulfilment runtime implementation slice may process verified test-mode Store payment receipts through `POST /api/stripe/store-webhook` only when local/test Store sandbox flags and `DZN_STORE_WEBHOOK_FULFILMENT_ENABLED=true` are explicitly enabled. Store entitlements remain private account-bound cosmetic/supporter records and remain separate from owner Starter/Pro billing. The runtime may write only `store_fulfilment_attempts`, `store_order_status_history`, `account_entitlements`, `store_entitlement_status_history`, optional `supporter_cards`, and `store_refund_dispute_audit`. It must not mint earned spins, run the reward wheel, enable live checkout, mutate Stripe Products/Prices, mutate Cloudflare config, write production D1, or change issue #49. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store fulfilment reconciliation/read-model preflight slice is documentation/test-guard work only. It may define future private Account Purchases and Entitlements read models, private Supporter Card reveal/status UI, admin-only webhook replay controls, admin-only manual-review controls, and admin-only refund/dispute operator workflow in `docs/DZN_STORE_FULFILMENT_RECONCILIATION_READ_MODEL_PREFLIGHT.md`. It must not add Account Purchases routes, Entitlements routes, public Supporter Card reveal, webhook replay routes, manual-review routes, refund/dispute operator routes, notifications, migrations, production D1 applies, live checkout activation, earned-spin ledgers, reward wheel runtime, Stripe Product/Price/customer/refund/dispute/webhook endpoint mutation, Cloudflare secret/config mutation, production D1 writes, or issue #49 changes. Future customer read models must be private/no-store and scoped to the current authenticated user. Future operator read/action models must require configured DZN admin/operator scope, not owner Starter/Pro entitlement. The preflight cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store Account Purchases read-model implementation adds `GET /api/account/purchases` as a disabled-by-default private account route. It requires `DZN_STORE_ACCOUNT_PURCHASES_READ_MODEL_ENABLED=true`, `DZN_STORE_ENABLED=true`, local/test Store sandbox runtime, live checkout flags false, earned-spin/reward-wheel flags false, and a valid DZN session before returning any purchase data. It reads only the current user's sanitized Store ledgers for `livemode = 0` and the active local/test sandbox ledger scope, and returns no raw Stripe ids, payment method data, billing address, raw Discord ids, raw internal DZN user/order/item/entitlement/card ids, Supporter Card serial numbers, webhook raw bodies, raw provider payloads, operator notes, other sandbox-scope rows, or other users' Store records. It adds no public Supporter Card reveal, no Account Purchases UI page by itself, no Entitlements route, no operator route, no notification, no migration, no production D1 apply, no live checkout activation, no earned-spin ledger, no reward wheel runtime, no Stripe mutation, no Cloudflare config mutation, no production D1 write, and no issue #49 change. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, or competitive eligibility.

The DZN Store Account Purchases UI shell adds `/account/purchases` as a private read-only account page over `GET /api/account/purchases`. It sends included session credentials, uses no-store fetching, redirects `401` users to login, and can render an unavailable state while the read model is disabled by default. It must not call Store order, Store checkout, Store webhook, Store operator, billing, wheel, profile-privacy, progression, review, event, ranking, analytics, or tracking endpoints. It shows no Supporter Card serial numbers or generated card art and adds no Supporter Card reveal, Entitlements route, operator route, notification, migration, production D1 apply, live checkout activation, earned-spin ledger, reward wheel runtime, Stripe mutation, Cloudflare config mutation, production D1 write, or issue #49 change.

The DZN Store Supporter Card reveal approval preflight slice is documentation/test-guard work only. It may define the future private `GET /api/account/supporter-cards/[cardRef]/reveal` contract, account ownership proof, serial/art redaction boundaries, visibility controls, screenshot/export rules, audit requirements, rollback path, and security proof in `docs/DZN_STORE_SUPPORTER_CARD_REVEAL_APPROVAL_PREFLIGHT.md`. It must not add a card reveal route, private reveal component, public reveal, card-art generation, sharing controls, screenshot/export controls, notification route or writes, migration, production D1 apply, live checkout activation, earned-spin ledger, reward wheel runtime, Stripe mutation, Cloudflare config mutation, production D1 write, or issue #49 change. Future private reveal must be authenticated, current-user scoped, private/no-store, local/test only until separate live Store approval, and must prove ownership through Store order, order item, account entitlement, and Supporter Card ledger joins. It cannot affect billing, owner entitlement, server ownership, rankings, discovery score, reviews, review score, badges, seasons, events, CTF scoring, Server Wars scoring, XP awards, earned calling-card awards, public profile visibility, retained exports, moderation decisions, earned spins, reward wheel state, or competitive eligibility.

`/api/owner/community-members`, `/api/owner/community-members/[candidateId]`, `/api/owner/community-members/bulk`, `/api/owner/community-members/export`, `/api/owner/community-members/notifications/read`, `/dashboard/community-members`, and `/owner/community-members` are owner/admin-only community member source management surfaces. Normal owners must pass the canonical owner entitlement boundary and can manage only their own linked servers; configured DZN admins may manage the global source queue. The APIs may write `community_member_candidates`, `community_member_source_audit`, private `user_notifications`, trusted Discord/guild snapshot previews in `community_member_source_snapshots`, and imported `community_members` rows only after a candidate resolves to exactly one existing DZN user. Duplicate and ambiguous user bridges are rejected with audit history. Repeated no-match and repeated duplicate filters are admin review filters only. Selected-row bulk import/reject actions still recheck every row server-side through the canonical candidate action path before any row is imported or rejected. Per-candidate bulk execution summaries, filterable audit action/result grouping, and the export-safe owner/admin audit view are private read models derived from the same scoped source audit rows; they must not expose raw Discord IDs, actor user IDs, raw linked-server IDs, raw community guild IDs, or raw DZN user IDs in export-safe rows. `/api/owner/community-members/export` is a bounded downloadable export action for those export-safe rows only; it accepts linked-server, date/action/result filters, returns a private no-store CSV attachment, and performs no database writes. The dashboard may show client-session-only recent export history with generated time, selected filters, row count, and private owner/admin artifact status; that history is non-persistent by default, is clearable locally, and must not create a stored export file, export sharing link, or database export-history row. The dashboard may also show an owner/admin-visible export policy surface and optional retention settings display, but persistent retention is disabled unless a later approved slice adds expiry and audit controls; this policy surface must not add a retention setting write API, stored export file, sharing link, browser persistence, or database export-history row. Configured DZN admins may also see an admin-only policy review that confirms the current private, bounded, download-only, non-persistent export defaults across all owner scopes and states that future retained-export work is blocked until a dedicated approval, migration, expiry model, storage plan, and security review exist. The same admin-only review may show the retained-export approval design, but it is design-only: approval must come from the DZN platform owner with security reviewer and data retention owner review, must be recorded in a dedicated retained-export approval issue or PR, and must keep issue #49 reserved for live checkout activation. The design names the future retained-export migration shape, `future_retained_community_member_audit_exports.sql`, 7-day default retention, 30-day maximum retention, cron-secret-only deletion, private R2 bucket storage through `COMMUNITY_MEMBER_EXPORTS_BUCKET`, rollback rules, and proof requirements before any retained export files, export-history rows, sharing links, or retention write APIs are implemented. There are no retained export files, export-history rows, sharing links, or retention write APIs in this design-only slice. `community_member_candidate_importable` notifications may be created only for the linked-server owner when a candidate becomes importable, and private read/unread controls may mark only that owner/admin user's active community-member import alerts read. Public profile visibility still requires the player's opt-in generated handle and is not controlled by owner/admin source-management actions. These routes must not affect CTF scoring rows, owner workflow decisions, approval decisions, bracket outcomes, billing, rankings, discovery score, reviews, badges, seasons, Server Wars scoring, XP awards, calling-card awards, or competitive eligibility.

`/api/cron/player-progression/awards` is not a public or player endpoint. It is a cron-secret-protected trusted job for converting verified activity facts into player XP and calling-card awards. It may accept only explicitly verified source facts, process pending verified source rows, update existing player challenge participation progress/completion, and insert idempotent player XP/calling-card rows. It must not accept normal session auth, owner entitlement, Starter, Pro, server ownership, Nitrado, Stripe, or Discord bot permissions as a substitute for the cron secret. Progression awards must remain player profile progression only and must not affect rankings, discovery score, billing, server ownership, reviews, review score, events, tournaments, Server Wars scoring, badges, seasons, or competitive eligibility.

The protected progression award job may also collect verified source facts from trusted DZN activity adapters: ADM `player_events`, ADM `kill_events`, server event entries, and approved review/community activity. Those adapter reads can feed `player_progression_award_sources` and the same job can schedule failed source rows for retry. Adapter collection and retry must stay cron-secret-only, bounded, idempotent, and limited to progression-owned writes.

`GET /api/owner/progression/award-audit` is a private owner/admin audit surface, not a player progression mutation path. Normal owners must pass the canonical owner entitlement boundary and can read only award-source history tied to their own linked servers; configured DZN admins may read the global audit history. The route may show awarded, skipped, failed, progressed, duplicate, and pending source status with attempt/retry metadata, but it must be read-only, private/no-store, and must not expose raw evidence blobs, Discord IDs, billing state, Nitrado tokens, Discord bot tokens, Stripe secrets, or checkout configuration.

`/dashboard/progression-awards` and `/owner/progression-awards` are private owner/admin UI surfaces for the same audit route. They may filter by status, adapter key, linked server, and retry availability, but those filters must remain additive to the canonical owner/admin scope. The UI may display retry metadata and failed-row counts only; retry execution must remain cron-secret-only through `/api/cron/player-progression/awards` and must not be available as a browser action.

`/api/public/server-reviews` remains the public/read path for review summaries and approved review bodies. Logged-out users can receive a locked preview summary; logged-in players can read approved reviews and see their own review state. Public review responses must not expose reviewer Discord IDs or owner reply author user IDs.

`/api/public/server-reviews/[reviewId]/report` is a logged-in player moderation hook. It may write a report row, increment the reported review count, and move a repeatedly reported review to pending review. It must not write billing, ownership, ranking, discovery, badge, season, event, or competitive tables.

Owner replies are server-management actions. `PUT`/`DELETE /api/servers/[serverId]/reviews/[reviewId]/reply` must stay behind the owner entitlement boundary and existing server owner/admin checks. Owner replies may be displayed publicly with approved reviews, but they must not change the review rating, review count, average, ranking, discovery score, badge eligibility, seasons, events, or competitive eligibility.

`/dashboard/reviews`, `/owner/reviews`, and `/api/reviews/moderation` are private moderation surfaces. Normal owners must pass the canonical owner entitlement boundary and can only see or act on reviews for their own linked servers. Configured DZN admins can triage across servers through the same API. Moderation may approve, hold, remove, dismiss reports, save owner replies, remove owner replies, read safe status history, show owner dashboard badges, and create internal DZN Pulse notification rows. It must not change ratings, rankings, discovery score, billing, server ownership, badges, seasons, events, Server Wars, challenge outcomes, XP, calling cards, or competitive eligibility.

`POST /api/reviews/moderation/bulk` is stricter than the normal queue. It is DZN-admin-only and only accepts repeated report patterns returned by the private moderation API. It may update matching review moderation state, append moderation audit rows, and create internal DZN Pulse owner notifications. It must not be available to normal owners, must not expose actor user IDs or Discord IDs in status history, and must not write billing, ownership, ranking, discovery, badge, season, event, or competitive tables.

`POST` `/api/reviews/moderation/notifications/read` is the private review-alert read-state endpoint for the owner dashboard and review moderation queue. Normal owners must pass the canonical owner entitlement boundary, configured DZN admins may clear their own admin review alerts, and normal free players must be rejected. The endpoint may update only unread, unexpired `user_notifications.read_at` rows belonging to the authenticated owner/admin and matching review moderation notification types or `/dashboard/reviews` action URLs. Review notification read state is private per owner/admin, does not clear general DZN Pulse alerts, does not send Discord notifications, and must not affect billing, rankings, discovery score, review score, badges, seasons, events, Server Wars, challenge outcomes, XP, calling cards, or competitive eligibility.

Event browsing, event detail pages, event suggestions, votes, and reports remain player/community surfaces. Registering or matching an owned server for an event is an owner action and must cross the billing entitlement boundary first.

These APIs must keep their existing preview redaction and `Vary: Cookie` behavior where applicable. Public API availability does not mean the corresponding app page is public.

## Production Verification

Post-merge verification should expect:

- `/` returns `200`.
- `/pricing` returns `200` and opens the dedicated Starter/Pro pricing page.
- `/servers/preview/community` returns `200` as the public-safe community member shell.
- Logged-out direct app pages such as `/player`, `/events`, `/leaderboards`, `/servers`, `/dashboard`, `/setup`, `/dzn-pulse`, and `/seasons` return a login redirect.
- Logged-in free players can open player surfaces such as `/player`, `/events`, `/leaderboards`, `/servers`, `/dzn-pulse`, and `/seasons` without payment.
- Logged-in free players who open `/setup` or `/dashboard` are redirected to the dedicated owner pricing page.
- Logged-out header/navigation does not expose app/product controls, while authenticated headers show player/owner package-appropriate actions.
- The dashboard sidebar shows package-aware guidance: trial-safe tools, Pro locks/upgrade prompts, or active Pro tools based on the authenticated account summary.
- Public APIs above return `200` and no unexpected 5xx.
- Owner/protected APIs such as `/api/owner/events`, `/api/billing/status`, and `/api/nitrado/services` remain `401` without authentication. Owner-management APIs return owner-plan-required errors for authenticated free players.
- No production D1, Stripe, Nitrado, Discord, or secrets mutation is required for this policy.
