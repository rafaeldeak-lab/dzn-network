DZN server theme banner assets live here.

Theme banner image assets can use keys such as `chernarus.webp`, `winter.webp`, `neon-city.webp`, and `space.webp`. CSS fallback gradients are used when images are not available.
